# Python revision:
  -- to run a python script `python filename.py`
## Quick keys:
  -- open and close terminal `ctrl + ~`
  -- clearing or cleaning the terminal `ctrl + l`
  -- to view previous terminal commands cycle `up - arrow` or `down-arrow`
  -- to taggle a comment `ctrl + ?`

  ## Build-it functions:

    -- `print`